name: cloud_env
description: Cloud Security Misconfiguration Environment

tasks:
  - id: easy
    description: Fix public S3 bucket
    grader: grade_easy

  - id: medium
    description: Close open SSH port
    grader: grade_medium

  - id: hard
    description: Fix IAM privilege escalation
    grader: grade_hard

entrypoint:
  module: server
  object: app